name: Regression Testing

on:
  push:
    branches: [ main, develop, MM2, 'jules-*' ]
  pull_request:
    branches: [ main, develop, MM2 ]

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libnetcdf-dev libhdf5-dev

    - name: Install Core Repositories
      run: |
        pip install --upgrade pip
        pip install git+https://github.com/bbakernoaa/monetio.git@develop
        pip install git+https://github.com/bbakernoaa/monet.git@feature/interp_improvements
        pip install git+https://github.com/bbakernoaa/monet-stats.git@dev
        pip install git+https://github.com/bbakernoaa/monet-plots.git@main

    - name: Install MELODIES-MONET
      run: |
        pip install -e .
        pip install prefect prefect-dask dask-jobqueue dask-cloudprovider networkx pooch pytest pytest-mpl xarray pandas numpy

    - name: Run Regression Tests
      run: |
        python -m pytest melodies_monet/tests/test_regression.py
